# Public browser API origin at BUILD time; blank uses same-origin /api proxy.
NEXT_PUBLIC_API_URL=
# Server-only upstream at BUILD time (Compose uses http://backend:8000).
API_PROXY_URL=http://127.0.0.1:8000
